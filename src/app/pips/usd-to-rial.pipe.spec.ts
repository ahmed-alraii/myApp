import { UsdToRialPipe } from './usd-to-rial.pipe';

describe('UsdToRialPipe', () => {
  it('create an instance', () => {
    const pipe = new UsdToRialPipe();
    expect(pipe).toBeTruthy();
  });
});
